import {Token, TokenOptions} from 'aws-sdk/lib/token';

export class StaticTokenProvider extends Token {
    /**
     * Creates a new StaticTokenProvider object.
     */
    constructor(options?: TokenOptions);
}